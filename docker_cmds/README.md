# TensorRT experiments with docker container

## Pulling and running docker cmds
* Refer [pull_container.sh](pull_container.sh) to pull the docker image and [start_container.sh](start_container.sh) to start the docker image to perform TensorRT experiments