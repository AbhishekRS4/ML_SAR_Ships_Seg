# TensorRT experiments with docker container

## Pulling and running docker cmds
* Refer [pull_tensorrt_container.sh](pull_tensorrt_container.sh) to pull the docker image and [start_tensorrt_container.sh](start_tensorrt_container.sh) to start the docker image to perform TensorRT experiments